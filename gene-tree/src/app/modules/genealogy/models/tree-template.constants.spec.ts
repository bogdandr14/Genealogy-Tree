import { TreeTemplateConstants } from './tree-template.constants';

describe('TreeTemplateConstants', () => {
  it('should create an instance', () => {
    expect(new TreeTemplateConstants()).toBeTruthy();
  });
});
